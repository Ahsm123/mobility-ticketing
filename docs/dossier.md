Assumptions:
    A route can contain the same stop multiple times, but never two times in a row.

    On busses and trams users can validate their own tickets when onboarding, on trains the operators validates on demand.
